<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CoffeeTime" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CoffeeTime" parent="Base.Theme.CoffeeTime" />
    <style name="TimePickerDialogStyle" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColorSecondary">@color/black</item>
        <item name="android:colorControlActivated">@color/mainColor</item>
    </style>
    <style name="DatePickerDialogStyle" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">#FFCA42</item> <!-- Specify your desired color here -->

    </style>
    <style name="ThemeOverlay.Toobar" parent="ThemeOverlay.MaterialComponents.Toolbar.Primary">
        <!-- Text color -->
        <item name="android:editTextColor">@color/backCardColor</item>
        <!-- Hint text color -->
        <item name="android:textColorHint">@color/backCardColor</item>
    </style>
</resources>